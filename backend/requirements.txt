fastapi>=0.111.0
uvicorn[standard]>=0.29.0
openpyxl>=3.1.2
pydantic>=2.7.0
python-multipart>=0.0.9
# CP-SAT optimal solver (Google OR-Tools) — for slow-but-optimal mode
ortools>=9.10.0

# ─── Database (Sprint 1) ─────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0          # async driver for FastAPI runtime
psycopg2-binary>=2.9.9   # sync driver for Alembic migrations
alembic>=1.13.0
python-dotenv>=1.0.0     # load .env in local dev (no-op on Railway)

# ─── Tests (Sprint 1) ────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0            # ASGI client for in-process FastAPI testing
